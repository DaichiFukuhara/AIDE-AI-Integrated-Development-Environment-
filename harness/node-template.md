---
id: <ドット区切り。例 auth.login>
parent: <親の id。根なら null>
depth: <0-3。0=根 / 1=課題 / 2=解決法の詳細化と機能 / 3=システム（葉）>
children: []
status: draft          # draft / questions-done / decision-approved
                       # / split-proposed / split-prechecked / split-approved
                       # / children-created / done
                       # / deferred / rejected / stale
seams: []              # 子同士の接続。子がいなければ空
uses_seams: []         # 親が定義した seam のうち、自分が使うもの
---

# <ノード名>

<!--
深さごとの対象:
- 0: ペイン・目的・利用者・成功条件・不変条件を定義する根
- 1: 解くべき課題の範囲と、それに対する解決法
- 2: 親の決定の詳細化と、それに伴う機能
- 3: 実装するシステムの境界・振る舞い・受け入れ条件（葉）
-->

## 親からの振り分け

<!-- 分割承認時に親が転記する。子は書き換えない。根には無い。
     間違っていると判断したら、自分で直さず boundary_request で境界の所有者へ返す -->

- 責任:
- 詳細化の対象:
- 継承する制約:
- 割り当てられた受け入れ条件:
- uses_seams:
- parent_decision_ref:

## 1. これは何を決めるものか

## 2. なぜ要るのか

## 3. 誰が使い、誰が影響を受けるか

## 4. 終わったとどうやって分かるか（受け入れ条件）

- [ ]

## 5. 変えてはいけない前提

<!-- 祖先から降ってきた制約と seam。継承したものは出所を書く -->

## 6. 決めないこと・任せること

<!-- 実装時に決めてよいもの。ここが空だと実装者が止まり続ける -->

## 7. 分からないこと・仮置きしていること

## 分割提案

<!-- 深さ0-2だけ。roles/split.md の形式で追記され、前検査の結果もこの下に付く -->

## boundary_requests

<!-- 子孫からの境界変更の起票。追記のみ。open は非権威的で、子は継承しない -->

## seam_inbox

<!-- 子同士の seam 成果物はここへ追記され、親が権威化して宛先の子へ降ろす
     （根の split-4「親に残すもの」9）。子は兄弟のファイルに直接書かない。
     2026-08-31 追加: split-4 が搬送路として決めたのにテンプレートに無かった
     （precheck の指摘）。bootstrap 例外の範囲での修正 -->

## 承認証跡

<!--
決定の承認と分割の承認を分けて記録する。
版参照 `<node-id>@<日付>-<種別>-<連番>` / 承認者 / 日時 / 承認種別（decision / split）
/ 承認者の種別（人間）/ status（active / superseded / pending）
-->
