# CIV3-010〜012に対する手動照合

主担当による修正後のMarkdown規約の静的照合。台帳エンジンの実行テストではない。
前回の16ケースとPythonの実行結果は上書きせず保持する。

| ケース | 入力と操作 | 規約から追った結果 | 照合 |
| --- | --- | --- | --- |
| Q / CIV3-010 | Xのc1が期限超過。P1がmajorでrequire-review。Xの保留未解除。無関係Zの作業開始、cycle_closedを反復 | 起動前に完了済みも同subject/scope/phase/criteria/change_idsで検索。P1への対応付けと修正待ちを保存し、新しいP2や費用予約を作らない。通知ackは返せても差分・期限・保留は残す | pass |
| R / CIV3-010 | Qの後にXの修正候補S2が完成 | 新subjectと前回P1・指摘・差分を結び、限定再監査を起動。無関係Zの変更だけではXを再起動しない | pass |
| S / CIV3-010 | 監査の通信だけが失敗し結果不明、後に通信回復 | 元要求をin-flightのまま照会。結果確定を優先し、自動で新周期要求にしない。物理再実行が必要なら回復証拠と新予約が必要。重大設計指摘の残存にはこの例外を使えない | pass |
| T / CIV3-011 | Xの基準K1。dX1をdaily-passで採用、c1を記録。dX2の意味変更を正式adoption監査へ | 交わるc1を必須で選び、新差分c2にも事前IDを付ける。baseline_refs[X]=K1、累積差分はK1→dX1→提案S2。合格と採用でc1/c2を解消し、X基準をS2へ同時更新。残差分なしで次の周期はskip | pass |
| U / CIV3-011 | Tで要求からc1を漏らした合格結果が届く | 基準を進めるIDに関係する既存項目の漏れを記録役が検出。基準更新・採用を受理せず、不足を含めた要求へ戻す | pass |
| V / CIV3-011 | 旧版運用などによりlatest基準S2だが、c1に旧基準K1が残る | 項目自身のK1を起点とし、その旧新版を含む履歴を固定する。S2→currentの空差分にしない。K1保存物が欠ければblockedで復旧を要求。項目があるのに差分なしskipにしない | pass |
| W / CIV3-011 | X/Yの別起点から共同変更が合流する | 共同affected_scopeへ閉包し、baseline_refsをscope別に保存。比較不能な起点を日時だけで一つに潰さず、両経路と共同条件を確認。保証対象外の項目は保持 | pass |
| X / CIV3-012 | 次の試行に予約が必要。ローカル担当と外部実装作業者がいる | ローカル担当は学習役で予算定義と試行開始を判断し、記録役へ切替えて最新使用・予約を再照合しstateへ予約。外部作業者には予約済み一実行だけを委任し、結果を受けて記録役が精算。学習側や外部作業者が別writerとしてstateを直接更新しない | pass |

変更は規約・テンプレートに限る。snapshot.py・test_snapshot.py・製品演習は前回の15件/5件成功時と同じhashであることをpackage.jsonで照合し、無変更のテストは再実行しない。
実際の費用予約、台帳更新、周期監査全経路の自動運転、実ユーザー運用効果は未実証。
