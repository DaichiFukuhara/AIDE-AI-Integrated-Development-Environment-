# CIV3-013〜015の手動照合

修正したMarkdown実行規約・役割・テンプレートを主担当が静的に照合した。自動台帳エンジンの実行結果ではない。

| ケース | 状態と操作 | 規約上の結果 | 照合 |
| --- | --- | --- | --- |
| Y / 013 | 利用者が上限10を6へ下げる。学習の新定義は6、stateの写しは10、使用4、次回上限3 | 最新受領済み委任とdefinition_ref/写しが不一致なので保留。記録役が新参照と写しを同期後も4+3>6なので開始しない | pass |
| Z / 013 | 予約後、送信前に上限が変わる | 定義更新と未送信予約の再判定を同一state更新で保存。送信直前にも照合し、旧予約を根拠に実行しない。実行中だけは次の作業境界で停止する | pass |
| AA / 013 | 定義参照が読めない、または親口座の写しだけ古い | 適用される全口座の照合に失敗し、held/paused。使用実績や予約を消して残枠を作らない | pass |
| AB / 014 | 実験終端・cycle_closedのack済み、周期判定は失敗要求への修正待ち | recordは3番目の結果として対応付けを保存。orchestrateは「実験は終了、サイクル全体は未完了」とし、要求・担当・解除条件を引き継ぐ | pass |
| AC / 014 | 修正用新サイクルの後続監査が元の保留項目を保証して受理された | 元通知を後続結果へ対応付け、必要条件を満たしたときだけ元サイクルを完了にする。修正を始めるために元サイクルの完了を要求しない | pass |
| AD / 015 | K1が欠落しているがバックアップから同一hashを復元可能 | 通常の累積差分監査へ戻る。別内容の復元やinitialへの短絡をしない | pass |
| AE / 015 | K1復元不能。current B2と必要な現行証拠は全てある | loss recordを固定し、c1と依拠先にscopeを閉じる。新subjectにrecovery_refを含め、baseline_refs=initialの独立periodicを一度起動。合格までc1/期限/保留を維持 | pass |
| AF / 015 | AEの全体再検証が合格、currentも不変 | phase/change_ids/全scope被覆等を照合。旧新版比較の代わりに全体再検証を受理し、c1をrevalidated-after-lossで履歴化。新基準・解消・該当保留解除・通知対応を一括保存。歴史的事実の未確認は残す | pass |
| AG / 015 | 回復監査中にcurrentが変わる、またはc2が後着 | 対象版不一致ならstale。対象外/後着c2は解消しない。元の喪失保留と証拠を維持し新subjectへ戻る | pass |
| AH / 015 | 回復先でも現行実装・必須評価証拠がない、または別のmajorが残る | 入力の復元/再検証と指摘解消が必要。喪失だけを理由にN/Aや合格へ読み替えず、解除しない | pass |
| AI / 015 | 同じ回復通知の再送、作業開始の繰返し | 同一recovery_id/subjectの要求を対応付けて重複起動しない。既知の失敗を新しい回復IDで隠さず元結果と結ぶ | pass |

Pythonの実装・テスト・製品演習はこの変更では修正していない。既存15テストと製品5テスト/CLIの証拠は、実行時とのhash一致を確認したうえで引き継ぐ。
実課金・台帳の自動更新・運用効果の実証は今回の静的シナリオの対象外。
