<!-- aide:pool-archive
統合済み/差し戻し済みエントリの監査証跡。消さない。
-->

<!-- aide:entry
id: 72b403a8
lane: lanes/ObserveSystem.md
section: Observeシステムの改善案
accepted: 2026-08-03 18:34
report: reports/ObserveSystem-4.md
observeLevel: light
status: integrated
integratedAt: 2026-08-03 18:43
-->
## Observeシステムの改善案

Observeの現状のシステムが厳しすぎて進まないんだけどどういう風に改善するべきかな。

> ❓ **AI**: ❓ **AI**: 「厳しすぎて進まない」の具体像を教えてください。たとえば、(a) 矛盾検出が過剰でaccept不可になる、(b) 分割可能性チェックが細かすぎる、(c) レポート生成に時間がかかる、(d) `--force`を常用してしまう…のうちどれが一番効いていますか？（2026-08-03）（2026-08-03 00:50）

分割可能性を具体化しすぎている気がする。重要なシステムについてはこれでもいいんだけど、そこら辺を柔軟にして決めたいときとか早く始めたいときにこれって微妙じゃないかなって話。

> ❓ **AI**: ❓ **AI**: 「柔軟にしたい／早く始めたい」ケースを、観察の"厳しさ"をどの軸で緩めたいですか？ (a) 分割可能性チェック自体をスキップできるモード（例: `observe --quick` で矛盾のみ見る）、(b) レーン単位で厳しさレベルを宣言（例: ヘッダに `observe-level: light|strict`）、(c) 分割可能性を「不合格」ではなく「助言」に降格して accept をブロックしない、のどれが近いですか？（2026-08-03）（2026-08-03 00:53）

分割可能性をスキップできるのもいいし、厳しさレベルを最初に決められるのもいいね。cに関しては当初の理念を無視しているように思える。

> ❓ **AI**: ❓ **AI**: 方針が見えてきましたね。両立させるなら「デフォルトはstrict、ヘッダで`observe-level: light`宣言時のみ分割可能性チェックをスキップ」という形が近いでしょうか？ それとも「`observe --quick`のように実行時フラグで都度切り替える」方が運用しやすいですか？（2026-08-03）（2026-08-03 00:57）

ヘッダでできる方がいいかな。

> ❓ **AI**: > ❓ **AI**: ヘッダ宣言方式で進める場合、(a) キー名は `observe-level: light|strict` でよいですか？ (b) 宣言がないレーンのデフォルトは strict（=現状維持）で問題ないですか？ (c) `light` 指定時にスキップするのは「分割可能性チェックのみ」で、矛盾検出は従来通り走らせる想定でしょうか？（2026-08-03）（2026-08-03 01:04）

その3点で進めたい。キー名は `observe-level`、未指定は `strict`、`light` でも矛盾検出は必須にして分割可能性だけをスキップする。まずは二段階で始め、将来レベルを追加できる構造にはしておきたい。

> ❓ **AI**: ❓ **AI**: 「二段階で始めて将来レベルを追加できる構造」について確認させてください。(a) パーサ実装では `observe-level` を数値（例: light=10, strict=50）で内部保持し、閾値比較でチェック有無を判定する形が拡張しやすいですが、この方針でよいですか？ (b) 未知の値（例: `observe-level: draft`）が来たときは「エラーで停止」「警告してstrict扱い」「警告してlight扱い」のどれが望ましいですか？（2026-08-03）（2026-08-03 16:02）

> ❓ **AI**: ➕ **AI**: 方針が固まりましたね。将来のレベル追加を見据えると、内部的には `observe-level` を列挙値ではなく順序付きの段階（例: `light < standard < strict`）として扱う設計にしておくと、`strict` より厳しい `paranoid` や、`light` より緩い `draft` を後から差し込みやすくなりそうです。この方向で仕様化を進めてよいですか？（2026-08-03）（2026-08-03 15:59）

宣言場所は既存の `mdtalk protocol` ヘッダ内に統一したい。文書中の同名文字列は拾わず、先頭のプロトコルコメントにある `observe-level: light|strict` だけを有効にする。

> ❓ **AI**: ❓ **AI**: プロトコルヘッダ限定にする方針、明快でいいですね。実装イメージを固めるために確認させてください。(a) ヘッダは必ずファイル冒頭の最初の `<!-- mdtalk protocol ... -->` ブロックだけを対象にし、2つ目以降の同種コメントは無視する、で合っていますか？ (b) ヘッダ内での書式は現行の `mdtalk-models: ...` と同じ「1行1キー」スタイル（例: `observe-level: light`）に揃える想定でよいですか？（2026-08-03）（2026-08-03 16:03）

不正値や未知の値は黙って `strict` に戻さず、入力エラーとしてObserveを開始しない。タイプミスが厳格チェックを偶然回避する挙動は避けたい。

> ❓ **AI**: ❓ **AI**: 「入力エラーでObserveを開始しない」方針、タイプミス事故を防ぐ意味でよく分かります。エラー時の振る舞いについて確認させてください。(a) エラーメッセージには「許可される値の一覧（例: `light`, `strict`）」と「該当行番号」を含める想定でよいですか？ (b) 終了コードは他のObserveエラー（矛盾検出失敗など）と区別したいですか？ 例えば設定エラー専用のコードを割り当てると、CI側で「設定ミス」と「設計上の指摘」を切り分けやすくなりそうです。（2026-08-03）（2026-08-03 16:04）

`light` のレポートには、分割可能性を評価しなかったことを明示する。単なる `OK` ではなく「スキップ（observe-level: light）」と人にも機械にも分かる形にしたい。

> ❓ **AI**: ❓ **AI**: レポートへの明示方針、追跡性が上がっていいですね。表現の具体化について確認させてください。(a) 人間向けには「分割可能性: スキップ（observe-level: light）」のような日本語表記、機械向けにはJSON等の構造化フィールド（例: `{"split_check": "skipped", "reason": "observe-level=light"}`）を併記する想定でよいですか？ (b) `aide status` や `pool.md` のレーン索引側でも「lightで観察済み」と識別できるようにしますか？ 後段の統合判断で「厳密チェック未実施」を見落とさない仕組みがあると安心そうです。（2026-08-03）（2026-08-03 16:05）

`light` の verdict は矛盾検出の結果だけで決める。矛盾がなければ pass、あれば fail とし、未実施の分割可能性を pass 判定に混ぜない。

> ❓ **AI**: ❓ **AI**: verdictの決め方が明確になっていいですね。運用面で確認させてください。(a) `light`で矛盾なしの場合、レポートのverdict欄は `pass (split-check skipped)` のように「passだがスキップあり」を1フィールドで示しますか、それとも verdict は純粋に `pass`/`fail` のみとし、スキップ有無は別フィールドで持ちますか？ (b) 将来 `light` レーンを `strict` に昇格させたい場合、過去の `pass` を再検証するトリガー（例: `aide observe --recheck`）は必要そうですか？（2026-08-03）（2026-08-03 16:05）

`light` で pass したレポートも通常の `aide accept` を許可する。ただしレポートとプールのメタデータには使用レベルを残し、後から監査できるようにする。

> ❓ **AI**: ❓ **AI**: acceptを許可しつつ監査性を残す方針、バランスが取れていいですね。メタデータの持ち方について確認させてください。(a) レポート側とプール側の両方に `observe-level: light` を記録し、`aide integrate` でマスターへ統合する際もこの情報を引き継ぎますか？ それとも統合時点で「acceptされた＝十分」とみなし、マスターには残さない想定ですか？ (b) 後から「lightでpassしたレーンを一覧化したい」ケース（例: 監査時の再チェック候補洗い出し）を想定して、`aide status --observe-level light` のようなフィルタは必要になりそうですか？（2026-08-03）（2026-08-03 16:05）

`observe-level` を変更したら既存レポートは stale 扱いにする。レーン本文と同じハッシュ対象にヘッダが含まれる現在の仕組みを維持すればよい。

> ❓ **AI**: ❓ **AI**: stale扱いの運用について確認させてください。(a) staleになった既存レポートは `aide status` で「要再観察」として一覧化できるようにしますか？ (b) staleレポートに紐づくpoolエントリの扱いは、「acceptは維持するが再observeを推奨する警告を出す」「acceptを取り消してレーンに差し戻す」のどちらが望ましいですか？ レベルを`light`→`strict`に厳格化した場合と、`strict`→`light`に緩めた場合で扱いを変える必要はありそうですか？（2026-08-03）（2026-08-03 16:06）

既存レーンは宣言なしで `strict` になるので互換性を保てる。自動移行で既存ファイルを書き換える必要はなく、必要なレーンだけ明示的に `light` を追加する。

> ❓ **AI**: > ❓ **AI**: 「宣言なしで strict」の互換性方針、シンプルでいいですね。移行運用について確認させてください。(a) 既存レーンで `light` を後から追加した場合、その時点で既存レポートは stale 化する想定でよいですか？（ヘッダ変更＝ハッシュ変化なので自然にそうなりそう） (b) ドキュメント（README や docs/design）には「デフォルトは strict、`light` は明示宣言時のみ」を明記する必要がありそうですか？ 既存ユーザーが挙動変化に気づかず戸惑うのを避けたいところです。（2026-08-03）（2026-08-03 16:06）

CLIの開始ログと `aide status --json` に解決後のレベルを出したい。VS Code側も未観察・pass・failに加えて、どのレベルの結果か説明欄で確認できるようにする。

> ❓ **AI**: > ❓ **AI**: 可視化の粒度について確認させてください。(a) CLI開始ログは「observe-level: light (declared)」「observe-level: strict (default)」のように、宣言由来か既定由来かも区別して出しますか？ トラブル時に「宣言忘れなのか意図的な既定なのか」を切り分けやすくなりそうです。(b) `aide status --json` のフィールド名は `observeLevel` のようなキャメルケースで揃えますか、既存フィールドの命名規則に合わせたい意図はありますか？（2026-08-03）（2026-08-03 16:07）

`observe --quick` のような実行時フラグは今回は入れない。ファイルに残る宣言を唯一の指定方法にし、実行者によって判定条件が変わる事故を防ぐ。

> ❓ **AI**: > ❓ **AI**: 実行時フラグを排除する方針、判定の再現性が上がっていいですね。運用面で確認させてください。(a) 「今だけ試しに light で見たい」ケースが出た場合、一時的にヘッダを書き換えて observe→戻す、という手順を推奨しますか？ それとも「試したいなら宣言をコミットせよ」と割り切りますか？ (b) `AIDE_OBSERVER_CMD` などの環境変数でレベルを上書きする裏口も同じ理由で塞ぐ想定でよいですか？（2026-08-03）（2026-08-03 16:07）

実装ではヘッダ解析を `resolveObserveLevel(laneText)` のような純粋関数に分離し、`light` と `strict` の正規化済み値だけを返す形がよい。CLI・status・プロンプト生成で同じ関数を使いたい。

> ❓ **AI**: ❓ **AI**: 純粋関数として切り出す方針、テスタビリティが上がっていいですね。設計の細部について確認させてください。(a) `resolveObserveLevel(laneText)` の返り値は正規化済み文字列（`'light'|'strict'`）だけにしますか、それとも将来の段階追加を見据えて `{ level: 'light', source: 'declared'|'default' }` のようなオブジェクトを返す形にしておきますか？ 65行目で「宣言由来か既定由来か」を出したい話が出ていたので、後者だと呼び出し側で分岐せずに済みそうです。(b) 不正値検出（41行目）はこの関数内で例外を投げますか、それとも判定結果として `{ level: null, error: ... }` を返して呼び出し側にハンドリングを委ねますか？（2026-08-03）（2026-08-03 16:08）

観察者プロンプトは `strict` なら現行どおり矛盾と分割可能性の2項目、`light` なら矛盾だけを必須にする。モデルに分割可能性を考えてから無視させるのではなく、評価要求自体を明確に除外する。

> ❓ **AI**: ❓ **AI**: プロンプト分岐の実装方針について確認させてください。(a) `strict`用と`light`用のプロンプトを完全に別テンプレートとして持ちますか、それとも共通のベースプロンプトに「分割可能性セクション」を条件付きで差し込む形にしますか？ 後者だと将来レベル追加時にテンプレートが爆発せず済みそうです。(b) `light`のプロンプトでも「分割可能性は今回評価対象外」と明示的に書いてモデルに伝えますか？ 単に項目を消すだけだと、モデルが気を利かせて言及してしまう可能性がありそうです。（2026-08-03）（2026-08-03 16:08）

レスポンスJSONでは `separability.ok: null` より、`separability.checked: false` と `detail` を使う方が意図が明確だと思う。strictでは `checked: true` と `ok: true|false` を返させたい。

> ❓ **AI**: > ❓ **AI**: `checked: false` + `detail` の設計、意図が明示されていて良さそうですね。スキーマ定義について確認させてください。(a) `light` のとき `separability.ok` フィールド自体を省略しますか、それとも `ok: null` で明示的に「未評価」を残しますか？ 消費側（`aide status` や VS Code）が `ok` の有無で分岐するか、`checked` を必ず先に見るかで実装難度が変わりそうです。(b) `detail` には何を入れる想定ですか？ `light` なら `"observe-level=light のためスキップ"` のような固定文言、`strict` なら分割候補の説明、という使い分けでしょうか？（2026-08-03）（2026-08-03 16:08）

観察レポートのメタデータに `observeLevel: light|strict` を追加する。本文だけでなく `status --json` がレポートメタデータから安定して取得できる形式にする。

> ❓ **AI**: ❓ **AI**: メタデータの持ち方について確認させてください。(a) `observeLevel` は既存レポートのフロントマター（YAML）に追加しますか、それとも本文冒頭のHTMLコメント等に埋め込みますか？ 81行目の `separability.checked/ok` を含むJSON構造と同じ場所（例: レポート末尾の構造化ブロック）にまとめると、`status --json` の抽出ロジックが1箇所で済みそうです。(b) 67行目で議論した「宣言由来か既定由来か（source）」もメタデータに含めますか？ 監査時に「lightを明示宣言したレーン」と「strictがデフォルトで適用されたレーン」を区別できると、レビュー漏れ検知に使えそうです。（2026-08-03）（2026-08-03 16:09）

既存レポートには `observeLevel` がないので、読み込み時は `strict` と解釈する。過去レポートの表示やacceptを壊さず、新規レポートから明示的に書き出す。

> ❓ **AI**: > ❓ **AI**: 既存レポートを `strict` として解釈する互換方針、破壊せずに済んでいいですね。細部を詰めさせてください。(a) 87行目で議論した `source`（宣言由来／既定由来）は、既存レポートを読み込んだ場合どう扱いますか？ `source: 'legacy'` のような第三の値を用意して「宣言なし・既定適用」と区別しますか、それとも一律 `source: 'default'` として扱いますか？ 監査時に「本当にstrictで観察されたか」を遡って確認したい場面で差が出そうです。(b) 既存レポートを再observeした際、新レポートには当然 `observeLevel: strict` が書かれますが、それまでの間の staleness 判定（57行目）は「メタデータ欠落」と「ヘッダハッシュ不一致」のどちらを根拠にしますか？（2026-08-03）（2026-08-03 16:09）

単体テストでは、宣言なし・`strict` 明示の両方が分割可能性を要求することを確認する。既定値の互換性はプロンプト内容と生成レポートの両方で検証したい。

> ❓ **AI**: ❓ **AI**: テスト観点の具体化について確認させてください。(a) 「プロンプト内容の検証」は、`buildObserverPrompt(level)` のような関数の出力文字列に「分割可能性」セクションが含まれる／含まれないことをスナップショット的に確認する形でよいですか？ それとも文言の完全一致ではなく、キーワード（例: `separability`）の有無で判定しますか？ (b) 「生成レポートの検証」は実際にobserverコマンドを呼ぶ結合テストになりますか、それともモックレスポンスを与えてレポート書き出し部分だけを単体で確認しますか？ フィクスチャ（fixtures/mock-observer.js）を使う方針が既にあるなら、それに合わせたいところです。（2026-08-03）（2026-08-03 16:09）

`light` のテストでは矛盾チェック文言が残り、分割可能性を合否条件にしないことをモックへ渡したプロンプトで確認する。レポートに `checked: false` 相当が出ることも見る。

> ❓ **AI**: > ❓ **AI**: `light`テストの検証粒度について確認させてください。(a) 「矛盾チェック文言が残ること」はプロンプト文字列内のキーワード（例: `矛盾`や`contradiction`）の存在確認で十分ですか、それとも矛盾検出セクションが構造的に維持されていること（見出しや箇条書きの形）まで見ますか？ (b) 「分割可能性を合否条件にしない」ことの検証は、モックが `separability.checked: false` を返したときにverdictが `pass` になる（＝合否判定ロジックが分割可能性を参照していない）ことをアサートする形でよいですか？ プロンプト側だけでなく、レポート生成後の判定関数側もカバーしておくと退行を防ぎやすそうです。（2026-08-03）（2026-08-03 16:10）

未知値、空値、重複宣言もテストしたい。重複は最初を採用せずエラーにして、設定の曖昧さを残さない方針にする。

> ❓ **AI**: ❓ **AI**: 異常系テストの粒度について確認させてください。(a) 「重複宣言はエラー」の判定は、同一キー `observe-level` が2回以上現れた場合すべてをエラー扱いにしますか？ それとも「同じ値の重複」は許容し「異なる値の重複」だけをエラーにしますか？ 前者の方が厳格で曖昧さが残らない一方、コピペミスでの偶発的な重複も弾く挙動になります。(b) 「空値」は `observe-level:` のように値がないケースを指しますか、それとも `observe-level: ` (空白のみ) や `observe-level: ""` も含みますか？ テストケースとして列挙するときの境界をはっきりさせておきたいです。（2026-08-03）（2026-08-03 16:10）

結合テストでは `light` のpassをacceptできること、Observe後にヘッダを `strict` へ変えるとstaleでacceptできないことを確認する。この2点を既存のacceptテスト群へ追加し、`--force` の挙動は変えない。

<!-- done: (sonnet): 上の結合テスト方針について、残る確認事項を一つだけ挙げてください。 -->

> ❓ **AI**: ❓ **AI**: 「staleでacceptできないことを確認する」と「`--force`の挙動は変えない」の組み合わせが残る確認事項です。現行の`--force`がstaleチェックを迂回できるなら、105行目のテストは「`--force`なしの場合」に限定した話になります。結合テストのアサートは`--force`なし限定と明示しますか、それとも`--force`でもstaleはブロックされる（＝`--force`はstaleに効かない）という仕様でしょうか？（2026-08-03）（2026-08-03 16:13）

<!-- done: (sonnet): VS Codeとstatusでは、未観察でも現在の解決済みレベルを表示し、観察済みならレポートで使ったレベルも分けて表示する。現在値とレポート値が違えばstaleの理由を説明できる。この方針の不足を一つだけ指摘してください。 -->

> 💬 **AI**: 💬 **AI**: 指摘した不足点はこちらです。「現在値とレポート値が違えばstaleの理由を説明できる」とありますが、**observe-levelが同じままレーン本文だけが変わった場合**（例: 両方 `strict` のまま内容を編集）、UIには「現在値: strict／レポート値: strict」と表示されるだけで、なぜstaleなのかの説明が出ません。staleの根拠はハッシュ不一致（57行目）なので、レベル差分以外の原因（本文変更）によるstaleを人間にどう伝えるかが、この方針では抜けているように見えます。（2026-08-03）（2026-08-03 16:14）

<!-- done: (sonnet): `light` でも矛盾検出、レーン本文のハッシュ検証、accept前のstale検査は絶対に省略しない。緩和対象は分割可能性だけに限定する。この安全境界に抜けがあるか確認してください。 -->

> 💬 **AI**: 💬 **AI**: 安全境界の確認です。3点の潜在的な抜けがあります。（2026-08-03 16:15）
> 
> **① `--force` 迂回**（109行目の未解決点と直結）: `--force` が stale チェックを迂回できるなら「accept 前の stale 検査は絶対に省略しない」が崩れます。`--force` は stale に効かない、という仕様をここで明示しておかないと、安全境界の宣言と実装が食い違うリスクが残ります。
> 
> **② レポートファイル自体はハッシュ保護外**: レーン本文（ヘッダ含む）のハッシュは保護されますが、生成済みレポートファイルは保護されていません。レポートの `observeLevel` を手動で書き換えてもレーンのハッシュは変わらないため、stale 検査をすり抜けてしまいます。accept 時にレポートの `observeLevel` と現在のヘッダ宣言を突き合わせるコードチェックを追加すると、この穴が塞がりそうです。
> 
> **③ 矛盾検出の強制がプロンプト依存**: stale 検査とハッシュ検証はコードで強制できますが、`light` モードでの矛盾検出は「プロンプトでモデルに必須と伝える」だけです。モデルが矛盾検出を省略しても現状は検知できません。レポート JSON に `contradictions` フィールドの存在をコード側でも検証する仕組みがあると、安全境界がより確実になりそうです。（2026-08-03）

<!-- done: (sonnet): 実行ログには解決したレベルと宣言元（explicit/default）を出し、未知値エラーには許可値と宣言行を含めたい。トラブルシュートに必要な追加情報があれば一つ挙げてください。 -->

> 💬 **AI**: 💬 **AI**: 追加するなら**検出値（実際にパースされた文字列）**が最も効くと思います。エラーメッセージが「許可値: light, strict／宣言行: 5」だけだと、ユーザーはファイルを開いて5行目を確認する手間が残ります。「検出値: `lihgt`」を加えることで「どう書かれているか・何が正しいか・どこを直すか」が1行で揃い、ファイルを開かずに修正できます。正常系ログは `observe-level: light (explicit, 行5)`、エラー系は `不正値 'lihgt' を行5で検出 — 許可値: light, strict` という並びで情報を統一しておくと、ログの読み方に一貫性も出ます。（2026-08-03）（2026-08-03 16:16）

<!-- done: (sonnet): README、CLIヘルプ、VS Code READMEへ `light` と `strict` の違い、既定値、設定例、lightでも残るチェックを記載する。段階的ロールアウトは既定strictのまま始める。この文書化方針を確認してください。 -->

> ❓ **AI**: ❓ **AI**: 3箇所（README・CLIヘルプ・VS Code README）の記述内容は同一にしますか、それとも読者層に応じて粒度を変えますか？ 特に「CLIヘルプ」は `aide observe --help` の出力行に収まる短文を指しますか、それとも `docs/` 配下のMarkdown文書ですか？（2026-08-03）（2026-08-03 16:17）

<!-- done: (sonnet): 完了条件は、全既存テスト成功、追加単体・結合・VS Codeテスト成功、宣言なしの挙動不変、lightで分割可能性だけが非ブロッキングになることとする。受け入れ条件として不足があれば一つだけ挙げてください。 -->

> ❓ **AI**: ❓ **AI**: 一つ挙げます。**既存レポート（`observeLevel` フィールドを持たない旧フォーマット）を読み込んだ際に `accept` と `status` が正常動作すること**が完了条件に明示されていません。89行目で「既存レポートは `strict` と解釈する」と決まっていますが、「全既存テスト成功」のフィクスチャが旧フォーマットのレポートを含んでいるかどうかは別問題です。後方互換性の保証をテスト条件として明記しておかないと、旧レポートを読み込む経路が未検証のまま完了判定されるリスクがあります。（2026-08-03）（2026-08-03 16:18）

<!-- done: (sonnet): ここまでの合意を実装可能な最小仕様として確定したい。未解決点と実装順序を短く整理し、最後にObserve実行前に人間が決めるべき事項が残っているか示してください。 -->

> 💬 **AI**: 🧭 **AI**: ご依頼の整理です。（2026-08-03）（2026-08-03 16:19）
> 
> ---
> 
> ### 確定済み・実装してよい仕様（S1〜S11）
> 
> | # | 内容 |
> |---|------|
> | S1 | `resolveObserveLevel(laneText)` — ファイル冒頭の第一プロトコルブロックのみ解析、不正値は例外（検出値・許可値・行番号をエラーに含む） |
> | S2 | 未宣言 → `strict`、`light` 明示時のみ緩和（既存ファイル互換） |
> | S3 | `light` = 矛盾検出のみ必須、分割可能性はプロンプトから**除外を明示**してスキップ |
> | S4 | レポートJSON: `separability.checked: false + detail`（light）/ `checked: true, ok: bool`（strict） |
> | S5 | レポートメタデータ: `observeLevel: light|strict`。既存レポートは `strict` と解釈 |
> | S6 | `light` pass → `aide accept` 許可。レポート・プールに使用レベルを記録 |
> | S7 | ヘッダ変更 → ハッシュ変化 → stale（現行仕組み維持） |
> | S8 | 実行時フラグなし。ファイル宣言が唯一の指定方法 |
> | S9 | CLIログ: `observe-level: light (explicit, 行N)` / `observe-level: strict (default)` |
> | S10 | accept 時にレポートの `observeLevel` と現在ヘッダ宣言を突き合わせる |
> | S11 | レポートJSONに `contradictions` フィールドの存在をコード側でも検証 |
> 
> ---
> 
> ### 実装順序（推奨）
> 
> 1. `resolveObserveLevel` 純粋関数 + 単体テスト（S1・S2）
> 2. プロンプトビルダー分岐（S3）
> 3. レポートスキーマ追加（S4・S5）
> 4. accept / status の更新（S6・S7・S10・S11）
> 5. VS Code 表示更新
> 6. 結合テスト（light pass→accept、header変更→stale reject、旧フォーマット互換）
> 7. ドキュメント（README・CLIヘルプ・VS Code README）
> 
> ---
> 
> ### Observe実行前に人間が決める必要がある未解決点（4点）
> 
> **❶ `resolveObserveLevel` の返り値型**（75行目）
> 文字列 `'light'|'strict'` か、`{ level, source: 'declared'|'default' }` オブジェクトか。S9の宣言元表示を呼び出し側で分岐なく実現するにはオブジェクトが自然ですが、合意が取れていません。
> 
> **❷ `separability.ok` の扱い**（83行目）
> `light` 時にフィールドを**省略**するか **`null` で明示**するか。消費側が常に `checked` を先に読むなら省略でよいですが、未決定です。
> 
> **❸ `--force` と stale の関係**（109行目）
> `--force` が stale をバイパスできる現行仕様を維持するか変えるか。S10・S11の安全境界宣言との整合が必要です。
> 
> **❹ 重複宣言の判定基準**（101行目）
> 同一値の重複（例: `observe-level: light` が2行）も全エラーにするか、同値なら許容するか。
> 
> ---
> 
> この4点を決めれば実装に入れる状態だと思います。優先度が高いのは ❶（関数シグネチャが確定しないと呼び出し側全体が設計できない）と ❸（安全境界のコア部分）ではないでしょうか。

### 最終決定

未解決の4点は次で確定する。

1. `resolveObserveLevel` は `{ level: 'light'|'strict', source: 'declared'|'default' }` を返す。
2. `light` 時の `separability` は `{ checked: false, ok: null, detail: 'observe-level: light のためスキップ' }` とし、JSONの形を固定する。
3. `--force` は現行どおりstale検査を迂回できる。明示的な緊急回避手段として維持し、通常のacceptではstaleを必ず拒否する。
4. `observe-level` が複数あれば、同じ値の重複を含めて設定エラーにする。

以上を最小実装仕様とし、これ以上のレベル追加、ポリシー追加、例外ケースの詳細化は今回のスコープ外とする。
<!-- aide:end -->
