---
id: authoring.node
parent: authoring
depth: 2
children: []
status: draft
seams: []
uses_seams: [a2.form-gap]
---

# authoring.node（解決法: ノード1枚の中の著述形式）

<!--
深さ2で決めること: 親の決定の詳細化と、それに伴う機能。
親の決定を**詳細化する**のであって、選び直さない。
別案にしたいなら boundary_request で親へ返す。
-->

## 親からの振り分け

<!-- 親（authoring）が split-a1 の承認時に転記した。子は書き換えない。
     間違っていると判断したら、自分で直さず boundary_request で authoring へ返す -->

- 責任: ノード1枚の中で、決定1件がどう書かれ、決めるべき事項の母集団と未決がどう表れるかを決める
- 詳細化の対象: 著述モデルの5点のうち 1〜4（未承認案 / 承認後に決定 / 正本と版参照 / 4状態）。
  決定義務の**発生源の一覧を確定させ**、そこから母集団を閉じること。
  **持ち越された重大1件**: `a1.decision-form` が静的な状態しか運ばず、
  `承認` / `却下` / `撤回` / `差し替え` を区別する**遷移記録**（提案識別子・旧新版・事象）を
  `closure` へ渡す契約が無い。形式決定権を持つこのノードで決める
- 継承する制約: 根の不変条件1〜4と `boundary_requests` の例外、根の「親に残すもの」1〜15、
  ツール固有機能に依存しないこと（Codex / Claude の双方で回る）、予算（深さ3・子数5）。
  authoring の「親に残すもの」1〜10。とくに 3（形式決定権は node が単独で持つ）、
  9（競合する要求の優先規則）、10（4状態と失効 `stale` は直交する）
- 割り当てられた受け入れ条件: 所在 / 整合（欠落の側）
- uses_seams: `a2.form-gap`（`closure` から: 形式で足りないものの要求。受理／却下の応答を対で返す）
- 提供する seam: `a1.decision-form`（`closure` へ: 決定1件と未決1件の表現。1件が道具なしで通読できること）
- 親から降りる入力: `s3.gap` と `s5.invalidation`。**`uses_seams` には書かない。**親（authoring）が受けて
  権威化し、この節を更新して降ろす
- parent_decision_ref: `authoring@2026-08-31-decision-1`
  （祖先: `root@2026-08-29-decision-2` ＋ `root@2026-08-31-decision-3`
  ＋ `root@2026-08-31-decision-4` ＋ `root@2026-08-31-split-2`）

## 1. これは何を決めるものか

## 2. なぜ要るのか

## 3. 誰が使い、誰が影響を受けるか

## 4. 終わったとどうやって分かるか（受け入れ条件）

- [ ]

## 5. 変えてはいけない前提

<!-- 祖先から降ってきた制約と seam。継承したものは出所を書く -->

## 6. 決めないこと・任せること

<!-- 実装時に決めてよいもの。ここが空だと実装者が止まり続ける -->

## 7. 分からないこと・仮置きしていること

## 分割提案

## boundary_requests

## seam_inbox

## 承認証跡
