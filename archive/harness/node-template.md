---
id: <ドット区切り。例 auth.login>
parent: <親の id。根なら null>
depth: <0-3。0=根 / 1=課題 / 2=解決法の詳細化と機能 / 3=システム（葉）>
children: []
status: draft          # draft / questions-done / decision-approved
                       # / split-proposed / split-prechecked / split-approved
                       # / children-created / done
                       # / deferred / rejected / stale
seams: []              # 子同士の接続。子がいなければ空
uses_seams: []         # 親が定義した seam のうち、自分が使うもの
decision_ref:          # 現在有効な決定の版参照。例 root@2026-08-29-decision-2
split_ref:             # 現在有効な分割の版参照。深さ3は空
---

# <ノード名>

<!--
これは**設計書**。現在有効な決定だけを書く。
対話・分割提案・前検査・承認証跡・boundary_requests・seam_inbox は record.md へ置く。
どちらに書くかの判定は harness/README.md「設計書と記録を分ける」。

書かないもの: 履歴の痕跡（「旧: …」「…の指摘で改訂」）・却下された案・AI 自身の発言。
経緯は本文に書かず、版参照（decision_ref / split_ref）で record.md を引けるようにする。

深さごとの対象:
- 0: ペイン・目的・利用者・成功条件・不変条件を定義する根
- 1: 解くべき課題の範囲と、それに対する解決法
- 2: 親の決定の詳細化と、それに伴う機能
- 3: 実装するシステムの境界・振る舞い・受け入れ条件（葉）
-->

## 親からの振り分け

<!-- 分割承認時に親が転記する。子は書き換えない。根には無い。
     間違っていると判断したら、自分で直さず、親の record.md の
     `## boundary_requests` へ起票して境界の所有者へ返す -->

- 責任:
- 詳細化の対象:
- 継承する制約:
- 割り当てられた受け入れ条件:
- uses_seams:
- parent_decision_ref:

## 1. これは何を決めるものか

## 2. なぜ要るのか

## 3. 誰が使い、誰が影響を受けるか

## 4. 終わったとどうやって分かるか（受け入れ条件）

- [ ]

## 5. 変えてはいけない前提

<!-- 祖先から降ってきた制約と seam。継承したものは出所を書く -->

## 6. 決めないこと・任せること

<!-- 実装時に決めてよいもの。ここが空だと実装者が止まり続ける -->

## 7. 分からないこと・仮置きしていること

## 構成

<!-- 深さ0-2だけ。**分割が承認された時点で**、record.md の分割提案から
     この節へ昇格させる（roles/review.md）。承認前は空のままにする。
     ここが子の閉包になる。子は兄弟の設計書を読まず、この節だけを共有する -->

### 子

- `<child-id>`: <責任1文>

### seam

- id: <seam-id>
  from: <child-id>
  to: <child-id>
  内容: <何を渡すか>

### 親に残すもの

<!-- 子が共有する前提・共通制約。子はここを継承する -->

### 受け入れ条件の割り当て

| 親の受け入れ条件 | 担当 |
| --- | --- |
| <条件> | <child-id> または「統合時」 |

<!-- 子ごとの振り分けはここに残さない。各子の `## 親からの振り分け` へ転記する。
     提案本文・他の切り方との比較・前検査の指摘は record.md に残る -->
