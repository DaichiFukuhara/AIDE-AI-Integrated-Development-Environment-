# <ノード名> の記録

<!--
これは**記録**。設計書（index.md）から外したものを、すべてここへ置く。
対話・分割提案・前検査・承認証跡・却下された案・改訂の経緯・
boundary_requests・seam_inbox。

3つの規則:
- **追記だけ。**既存の記述を書き換えない（`status` の更新と `結果` の追記を除く）。
  記録はその時点の断面であり、現在の姿は設計書が持つ。
- **権威を持たない。**設計書と食い違ったら設計書が正。
  ここに書いただけでは、子にも実装にも降りない。
- **人間の承認を通ったものだけが、設計書へ昇格する。**昇格はその場で行う。

閉包（cold start で読むもの）にこのファイルは入らない。
開くのは、前検査・承認・「なぜそう決めたか」を追うときだけ。
-->

## 対話

<!-- 固定質問への回答をめぐるやりとり。AI 側の発言は `❓` `💬` `➕` `🔀` `🧭` を付ける。
     人間の答えそのものは設計書に書く。AI が設計書に効かせたい提案をした場合は、
     ここに書き、人間が採ったときに設計書の本文へ反映する -->

### <日付>

> 🧭 **AI**: <提案・観測・言い換えの提案>

## 分割提案

<!-- 書式は roles/split.md。承認前・却下・取り下げも含めてすべてここに残す。
     承認された提案の構成（子・seam・親に残すもの・受け入れ条件の割り当て）は
     設計書の `## 構成` へ昇格させ、ここには提案としての本文が残る -->

## 前検査

<!-- 書式は roles/precheck.md。失効した回も消さず、失効と分かるように残す -->

## 承認証跡

<!--
版参照 `<node-id>@<日付>-<種別>-<連番>` / 承認者 / 日時 / 承認種別（decision / split）
/ 承認者の種別（人間）/ status（active / superseded / pending）
重大指摘を残したまま承認した場合は、その理由（roles/review.md の要求）。
昇格させた先（設計書のどの節を書き換えたか）も1行で書く。
-->

## boundary_requests

<!-- 子孫からの境界変更の起票。追記のみ。`open` は非権威的で、子は継承しない。
     受理したら、その結果（新しい制約・seam）を設計書へ昇格させる。
     ここに accepted と書いただけでは子へ降りない -->

- from: <起票したノードの id> / date: <日付>
  対象: <動かしたい seam / owns / 責任範囲>
  理由: <観測した事実と、現状では決められない理由>
  status: open        # open / accepted / rejected

## seam_inbox

<!-- 子同士の seam 成果物はここへ追記される。親が権威化して設計書へ載せ、
     宛先の子の `## 親からの振り分け` へ降ろす。子は兄弟のファイルに直接書かない -->
