---
id: change.impact
parent: change
depth: 2
children: []
status: draft
seams: []
uses_seams: [c1.authorized-input]
---

# change.impact（解決法: 失効集合を決める純粋な関係）

<!--
深さ2で決めること: 親の決定の詳細化と、それに伴う機能。
親の決定を**詳細化する**のであって、選び直さない。
別案にしたいなら boundary_request で親へ返す。
-->

## 親からの振り分け

<!-- 親（change）が split-c1 の承認時に転記した。子は書き換えない。
     間違っていると判断したら、自分で直さず boundary_request で change へ返す -->

- 責任: 入力（封筒＋参照するツリーの版）から失効集合を一意に決める。根の失効表で分類できない変更は「判定不能」とする
- 詳細化の対象: 本文1の解決法のうち**失効集合を決める純粋な関係**。根の失効表の適用順序と適用条件
- 継承する制約: 根の不変条件1〜4と `boundary_requests` の例外、根の「親に残すもの」1〜15、
  ツール固有機能に依存しないこと（Codex / Claude の双方で回る）、予算（深さ3・子数5）。
  change の「親に残すもの」1〜11。とくに 2（判定はイベント単体ではなくイベント＋ツリー版）、
  3（根の失効表を選び直さない）、8（新しい失効範囲を発明しない）、
  9（fixture の5場合分けは親が固定し、期待値はこのノードが書く）
- 割り当てられた受け入れ条件: 失効集合の一意性 / 判定不能の検出
- uses_seams: `c1.authorized-input`（`propagate` から: 権威化された入力。封筒＋参照するツリーの版）
- 提供する seam: `c2.impact-set`（`propagate` へ: 失効集合、または「判定不能」とその理由。
  入力封筒 ID とツリー版参照を必ず含める）
- 親から降りる入力: なし。外部 seam の受信は `propagate` が担う
- parent_decision_ref: `change@2026-08-31-decision-1`
  （祖先: `root@2026-08-29-decision-2` ＋ `root@2026-08-31-decision-3`
  ＋ `root@2026-08-31-decision-4` ＋ `root@2026-08-31-split-2`）

## 1. これは何を決めるものか

## 2. なぜ要るのか

## 3. 誰が使い、誰が影響を受けるか

## 4. 終わったとどうやって分かるか（受け入れ条件）

- [ ]

## 5. 変えてはいけない前提

<!-- 祖先から降ってきた制約と seam。継承したものは出所を書く -->

## 6. 決めないこと・任せること

<!-- 実装時に決めてよいもの。ここが空だと実装者が止まり続ける -->

## 7. 分からないこと・仮置きしていること

## 分割提案

## boundary_requests

## seam_inbox

## 承認証跡
